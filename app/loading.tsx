export default function Loading() {
  return (
    <main className="system-state" aria-busy="true" aria-live="polite">
      <span>ANETO / SYNCHRONISATION</span>
      <h1>Je rassemble ce que je sais.</h1>
      <div className="system-progress" />
    </main>
  )
}
